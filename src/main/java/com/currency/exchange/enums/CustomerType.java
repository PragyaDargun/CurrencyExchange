package com.currency.exchange.enums;

/**
 * Type of customer.
 */
public enum CustomerType {
    /**
     * If customer is an employee.
     */
    EMPLOYEE,
    /**
     * if customer is a member.
     */
    MEMBER,
    /**
     * If customer is new.
     */
    NONE;
}
